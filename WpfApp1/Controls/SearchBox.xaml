<ContentControl x:Class="Controls.SearchBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Controls"
             mc:Ignorable="d" 
             BorderBrush="Black"
             BorderThickness="1"
             d:DesignHeight="100" d:DesignWidth="700">
    <Border Style="{StaticResource styleSearchBorder}">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox Grid.Column="0"
                 TextChanged="txtSearch_TextChanged"
                 Margin="13,0,0,0"
                 Style="{StaticResource styleSearchTextbox}"
                 x:Name="txtSearch"/>
        <Image HorizontalAlignment="Left"
               Margin="5"
               Source="/WpfApp1;component/Images/search2.png"/>
    </Grid>
    </Border>
</ContentControl>
